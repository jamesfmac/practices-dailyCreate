{
  "name": "practices-airtable",
  "version": "0.1.0",
  "description": "Retrieve and update practices from airtable",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -r dotenv/config  -e \"console.log(require('./index').handler({}));\"",
    "deploy": "./deployLambda.sh"
  },
  "author": "James McManamey",
  "license": "ISC",
  "dependencies": {
    "airtable": "^0.7.2",
    "moment": "^2.29.2",
    "moment-timezone": "^0.5.27"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
